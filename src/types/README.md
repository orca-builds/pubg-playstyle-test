# types

여러 파일에서 공유할 TypeScript 타입을 보관합니다.

타입은 질문, 선택지, 답변, 결과 등의 데이터가 어떤 모양이어야 하는지
정하는 규칙입니다. 실제 데이터는 `src/data`, 계산 함수는 `src/lib`에 둡니다.
향후 답변과 결과의 타입에는 테스트 버전을 연결할 수 있도록 설계합니다.

현재는 데이터 구조를 미리 확정하지 않고, 기능 구현 시 타입을 추가합니다.
